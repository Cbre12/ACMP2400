name: 'SBOM Generation and Scanning'
description: 'Generates a software bill of materials (SBOM) and scans for vulnerabilities'
author: 'colebreinig@'
outputs:
  sbom-file:
    description: 'the syft generated SBOM'
      value: ${{ steps.sbom-gen-and-scane.outputs.sbom-file }}
  
runs:
  using: 'docker'
  image: 'Dockerfile'
